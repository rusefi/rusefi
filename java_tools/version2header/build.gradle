plugins {
    id 'java-library'
}

defaultTasks 'jar'

jar {
    destinationDirectory = file( '$rootDir/../..' )
    manifest {
        attributes(
            'Main-Class': 'com.rusefi.Version2Header'
        )
    }
}