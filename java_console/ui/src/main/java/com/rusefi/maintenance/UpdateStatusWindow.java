package com.rusefi.maintenance;

import com.rusefi.ui.StatusWindow;

public class UpdateStatusWindow extends StatusWindow {
    public UpdateStatusWindow(String title) {
        showFrame(title);
    }
}
